language: python
cache: pip

python:
  - 3.7.1
  
install: #- pip install -r ...

script: 
  - true
  
notifications:
    on_success: change
    on_failure: change  # `always` will be the setting once code changes slow down
